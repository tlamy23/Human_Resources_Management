%html
  %head
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "Human Resources Management"
    %meta{:content => content_for?(:description) ? yield(:description) : "Human Resources Management", :name => "description"}/
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
    = yield :scripts
  %body
    .col-md-offset-2.col-xs-offset-2.col-md-8.col-xs-8
      %header
        = render 'layouts/navigation'
      %main{:role => "main"}
        = render 'layouts/messages'
    .col-md-offset-1.col-md-10.col-xs-offset-1.col-xs-10
      = yield

